<%= form_for(@public_album_image) do |f| %>
  <% if @public_album_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@public_album_image.errors.count, "error") %> prohibited this public_album_image from being saved:</h2>

      <ul>
      <% @public_album_image.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ImageID %><br>
    <%= f.number_field :ImageID %>
  </div>
  <div class="field">
    <%= f.label :AlbumID %><br>
    <%= f.number_field :AlbumID %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
