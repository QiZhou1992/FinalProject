<%= form_for(@public_album) do |f| %>
  <% if @public_album.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@public_album.errors.count, "error") %> prohibited this public_album from being saved:</h2>

      <ul>
      <% @public_album.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Name %><br>
    <%= f.text_field :Name %>
  </div>
  <div class="field">
    <%= f.label :FollowCount %><br>
    <%= f.number_field :FollowCount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
